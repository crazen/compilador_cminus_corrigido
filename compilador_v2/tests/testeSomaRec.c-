int v[5];
int soma_rec(int k) {
    if (k < 0) return 0;
    else return v[k] + soma_rec(k - 1);
}
void main(void) {
    int i;
    i = 0;
    while (i < 5) {
        v[i] = i + 1;
        i = i + 1;
    }
    output(soma_rec(4));
}
